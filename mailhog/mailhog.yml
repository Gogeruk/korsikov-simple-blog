# MailHog configuration file

# SMTP listener configuration
smtp:
  listen: ":1025"
  auth_disabled: true

# API listener configuration
api:
  bind_addr: ":8025"
  web_path: "/mailhog/"

# Storage configuration
storage:
  maildir_path: "/mailhog/data/maildir"
